import React from 'react'

type Props = {}

export default function WritePage({}: Props): JSX.Element {
    return (
        <div>
            <textarea></textarea>
            <button>글쓰기</button>
        </div>
    )
}
